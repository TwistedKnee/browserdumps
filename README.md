Following the release that Edge exposes cleartext saved passwords in memory plaintext [here](https://isc.sans.edu/diary/32954). My thought process was why not quickly write something that runs the dump in csharp. In addition, with manual review I have found chrome does the same thing. Why the code currently does chrome and edge. Adding more to that section can assist in dumping a processes memory. Mayhaps I will improve that, who knows. 

I am noticing that chrome doesn't always have passwords in plaintext in memory like edge. I had at first thought that was related to which process to dump. However it seems that if you visit the google password manager all of those are now plaintext. Which means you can dump them without a users password like you do if you have access to a browser. I wonder if making a quick request to chrome://password-manager/passwords in any context can load these in memory in plaintext. Seems like something to look into more. 
